# 待开发（Backlog）

> 说明：这里记录“还没做完 / 待验证 / 计划做”的功能点，避免遗漏。优先级按 P0 → P2。

## P0（当前优先）
- [ ] Windows 安装包构建后回归验证（包含翻译/分屏快捷键/标题颜色）
- [x] 应用内通知气泡：终端响铃/未读时在右下角堆叠提示，点击可跳转到对应标签页+分屏并清除未读
- [ ] PVE（Proxmox VE）多虚拟机屏幕墙：调研“现成工具 vs WaveCN 集成 Webview”的可行性与取舍（用户暂缓）

## P1（体验增强）
- [x] Git 面板：自检并清理可能存在的乱码文案
- [x] 顶部右侧“三点菜单”：自检并确认全部中文可读
- [ ] 左侧侧边栏：按钮顺序支持拖拽自定义
- [x] 终端滚轮滚动过快：已将 xterm `fastScrollSensitivity` 设为 1（如仍需更慢：再做设置项）
- [x] 终端滚轮逐行滚动：新增 `term:wheellinescroll`（默认 true），对鼠标滚轮按 1 行滚动（触控板仍走平滑滚动）
- [x] 终端按行滚动：方向键 `↑/↓` 在“已滚动离底部”时滚动 1 行
- [x] Windows `Alt+1` 切换到第 1 个标签页：主进程拦截 `Alt+数字` 并 reinject，规避 OS/IME 吞键
- [ ] 分屏状态色：无 Shell Integration 时目前仅按“最近输出(15s)”兜底，需更准确的忙闲判定（比如进程/工具链事件）

## P2（工程与交付）
- [ ] Linux 构建说明与安装命令整理
- [ ] 推送到用户远端仓库（按用户要求）
- [x] 主题/颜色：继续梳理并替换剩余硬编码颜色（Tab 指示器铃铛颜色改为 `var(--warning-color)`）
- [x] 独立整合目录：已复制 OpenCode/Codex 参考源码到 `third_party/opencode` 与 `third_party/codex`（不改原仓库，仅做本地参考）
- [ ] 独立整合原型：基于 `third_party/opencode` + `third_party/codex` 做集成验证（后续）

## 已取消/跳过（按用户确认）
- [ ] 文件管理器：复现并压测“长时间打开卡死”（用户确认现已不复现，暂不做）
- [ ] Codex：研究 `codex-restore-windows.zip` 里的“无审批/无沙盒”配置方式（用户要求跳过）

## 备注
- 快捷键约定：分屏焦点循环建议使用 `Ctrl+Tab` / `Ctrl+Shift+Tab`。
