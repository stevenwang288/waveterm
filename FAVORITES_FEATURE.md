# Favorites bar

Adds a simple Favorites bar under the tab strip:

- Add a file/folder to Favorites from the directory preview context menu (`Add to Favorites`)
- Add the terminal current working directory (CWD) to Favorites from the terminal context menu (`Add CWD to Favorites`)
- Click a favorite to open it in a new Files/Preview block
